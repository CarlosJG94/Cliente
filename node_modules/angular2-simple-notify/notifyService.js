"use strict";
var __decorate = (this && this.__decorate) || function (decorators, target, key, desc) {
    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
    else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
    return c > 3 && r && Object.defineProperty(target, key, r), r;
};
var __metadata = (this && this.__metadata) || function (k, v) {
    if (typeof Reflect === "object" && typeof Reflect.metadata === "function") return Reflect.metadata(k, v);
};
var core_1 = require('@angular/core');
var NotifyService = (function () {
    function NotifyService() {
    }
    NotifyService.addNotify = function (notify) {
        var timeout = (notify.timeout) ? notify.timeout : this.timeout;
        notify.active = true;
        NotifyService._notify.push(notify);
        setTimeout(function () {
            notify.active = false;
        }, timeout);
    };
    NotifyService.addPendingNotify = function (notify) {
        notify.active = true;
        NotifyService._pendingNotify.push(notify);
    };
    NotifyService.getAllNotify = function () {
        NotifyService._pendingNotify.forEach(function (n) {
            NotifyService.addNotify(n);
        });
        NotifyService._pendingNotify = [];
        return NotifyService._notify;
    };
    NotifyService.clearNotify = function () {
        var notifyIndex = [];
        NotifyService._notify.forEach(function (n, i) {
            if (!n.active)
                notifyIndex.push(i);
        });
        for (var i = notifyIndex.length - 1; i >= 0; i--)
            NotifyService._notify.splice(i, 1);
    };
    NotifyService._notify = [];
    NotifyService._pendingNotify = [];
    NotifyService.timeout = 3000;
    NotifyService = __decorate([
        core_1.Injectable(), 
        __metadata('design:paramtypes', [])
    ], NotifyService);
    return NotifyService;
}());
exports.NotifyService = NotifyService;
//# sourceMappingURL=notifyService.js.map